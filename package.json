{
  "name": "tempus-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "playwright": "^1.20.0",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "test": "playwright test tests --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.20.0",
    "lerna": "^4.0.0"
  }
}
