{
  "name": "tempus-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "typescript": "^4.1.6"
  },
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "test": "lerna run ci:test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
