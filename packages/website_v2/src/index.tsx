import React from 'react';
import ReactDOM from 'react-dom';
import { BrowserRouter } from 'react-router-dom';

import './index.scss';

ReactDOM.render(
  <React.StrictMode>
    <BrowserRouter>
      <div>Tempus Website V2.0</div>
    </BrowserRouter>
  </React.StrictMode>,
  document.getElementById('root'),
);
