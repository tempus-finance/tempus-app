describe('We need at least one test for CI to work properly - this should be removed once we add actual tests in the app.', () => {
  test('dummy test', () => {
    expect(true).toBeTruthy();
  });
});

export {};
